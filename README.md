Navigate to the root directory with command line
Run npm init to install the dependencies
Run webpack-dev-server --port 5000 (webpack dev server doesn't play nice
with port 8080 on Mac for some reason)
